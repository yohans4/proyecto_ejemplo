<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>
        <%= titulo %>
    </title>

    <!-- Vinculación del archivo CSS -->

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #817B04;
            /* Color de fondo */
            color: #fff;
            /* Color de texto principal */
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #333;
            /* Fondo del formulario */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            /* Sombra */
        }

        .container h2 {
            color: #fff;
            /* Color blanco para el título dentro del container */
        }


        label {
            display: block;
            margin-bottom: 10px;
            color: #fff;
            /* Color de las etiquetas */
        }

        input[type="email"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #A0A09E;
            border-radius: 4px;
            font-size: 14px;
            background-color: #A0A09E;
            /* Fondo de los campos */
            color: #333;
            /* Color del texto dentro de los campos */
        }

        textarea {
            resize: vertical;
            height: 150px;
        }

        input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 12px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
    <!-- Inclusión de partes comunes del head -->
    <%- include('partes/head') %>




</head>

<body>
    <!-- Inclusión de la navegación -->
    <%- include('partes/navegacion') %>

        <!-- Contenedor principal -->
        <div class="container">
            <h2>Contacto</h2>
            <form action="/enviar" method="POST">
                <label for="email">Correo Electrónico:</label>
                <input type="email" id="email" name="email" required>

                <label for="mensaje">Mensaje:</label>
                <textarea id="mensaje" name="mensaje" required></textarea>

                <input type="submit" value="Enviar">
            </form>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>